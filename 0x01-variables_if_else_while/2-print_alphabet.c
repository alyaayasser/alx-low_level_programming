#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: using for loop to print the alphabet
 *
 * Return: Always (0)
*/

int main(void)
{
char a;

for (a = 'a' ; a <= 'z' ; a++)
putchar(a);
putchar('\n');
	return (0);
}
